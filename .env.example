# Simple MCP Market Data Server Configuration
# Copy this file to .env and replace the placeholder values with your actual configuration

# Alpha Vantage API Configuration
# Get your free API key from: https://www.alphavantage.co/support/#api-key
API_URL=https://www.alphavantage.co
API_KEY=your_alpha_vantage_api_key_here

# MCP Server Implementation Details
# These values define how the server identifies itself to MCP clients
TITLE=Simple MCP Market Data
NAME=simple-mcp-market-data
VERSION=1.0.0

# Optional: Additional configuration examples
# Uncomment and modify as needed

# Server Configuration (if implementing custom server settings)
# SERVER_PORT=8080
# SERVER_HOST=localhost

# Logging Configuration (if implementing custom logging)
# LOG_LEVEL=info
# LOG_FORMAT=json

# Rate Limiting Configuration (if implementing rate limiting)
# RATE_LIMIT_REQUESTS_PER_MINUTE=25
# RATE_LIMIT_REQUESTS_PER_DAY=500

# Development/Debug Settings
# DEBUG=false
# VERBOSE_LOGGING=false

# Notes:
# - Never commit your actual .env file with real API keys to version control
# - The free Alpha Vantage tier allows 25 requests per day and 5 requests per minute
# - For production use, consider upgrading to a paid Alpha Vantage plan
# - Keep your API key secure and rotate it periodically
